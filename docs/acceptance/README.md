# Acceptance evidence

Dated slice-evidence files: `<YYYY-MM-DD>-<slug>.md`, one file per validation
pass, content verbatim (see `git log -- ACCEPTANCE.md` for provenance).

New slice evidence = new dated file here + one row in the `ACCEPTANCE.md`
table. Never append long logs to `ACCEPTANCE.md` itself.
